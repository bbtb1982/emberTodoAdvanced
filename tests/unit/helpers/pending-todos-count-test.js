import { pendingTodosCount } from '../../../helpers/pending-todos-count';
import { module, test } from 'qunit';

module('Unit | Helper | pending todos count');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = pendingTodosCount(42);
  assert.ok(result);
});
