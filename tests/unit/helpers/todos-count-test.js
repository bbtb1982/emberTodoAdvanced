import { todosCount } from '../../../helpers/todos-count';
import { module, test } from 'qunit';

module('Unit | Helper | todos count');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = todosCount(42);
  assert.ok(result);
});
